{
  "name": "clases",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npx cypress open --e2e -b chrome",
    "testing": "npx cypress open --e2e -b chrome --config baseUrl=https://pushing-front.vercel.app/",
    "desarrollo": "npx cypress open --e2e -b chrome --config baseUrl=https://pushing-it.vercel.app/"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cypress": "^10.3.0"
  },
  "devDependencies": {
    "cypress-xpath": "^2.0.1"
  }
}
